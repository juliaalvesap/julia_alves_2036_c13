%% Lista 4
clc;
clear all;
close all;

%% 4.1

M = 2;
B = 3;
K = 8;

num = 1;
den = [M B K];

G1 = tf(num, den);

G1

figure
step(G1, 15)

grid on

xlabel("tempo (s)")
ylabel("posição")
title("sistema massa mola amortecedor - caixa branca")

%% 4.2

R = 1000;
tau = 2;

C = tau/R;

disp("Valor da capacitância:")
disp(C)

num = 1;
den = [R*C 1];

G1 = tf(num, den);

disp("Função de transferência:")
G1

figure
step(G1, 10)

grid on

xlabel("tempo (s)")
ylabel("tensão (V)")
title("circuito RC - caixa cinza")

%% 4.3

clc;
clear all;
close all;

M = 4;
F = 1;
v = 0.5;

B = F/v;

disp("coeficiente de atrito:")
disp(B)

num = 1;
den = [M B];

G1 = tf(num, den);

disp("função de transferência:")
G1

figure
step(G1, 10)

grid on

xlabel("tempo (s)")
ylabel("velocidade (m/s)")
title("sistema massa-atrito - caixa cinza")
